---
import ButtonLink from "../../../primitives/button-link.astro";
import type { NavigationModuleProps } from "../../../renderer/module-definitions";

type Props = NavigationModuleProps;

const { businessName, links, cta } = Astro.props;
---

<header data-module-id="navigation-basic-v1" class="site-navigation border-b border-line/80">
  <nav
    class="page-container flex min-h-[var(--theme-nav-height)] flex-wrap items-center justify-between gap-x-8 gap-y-3 py-4"
    aria-label="Navegación principal"
  >
    <a href="#inicio" class="brand-mark touch-link text-xl">
      {businessName}
    </a>

    <div class="order-3 flex w-full items-center gap-6 text-sm font-medium sm:order-2 sm:w-auto">
      {links.map((link) => (
        <a class="nav-link transition-colors hover:text-accent" href={link.href}>
          {link.label}
        </a>
      ))}
    </div>

    <div class="order-2 sm:order-3">
      <ButtonLink href={cta.href} label={cta.label} variant="quiet" />
    </div>
  </nav>
</header>
