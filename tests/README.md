# Tests

Pruebas de schemas, fixtures, renderer y gates de preview.

La suite inicial debe cubrir al menos datos inválidos, `noindex`, etiqueta de concepto no oficial, enlaces/CTA y comportamiento responsive básico.
